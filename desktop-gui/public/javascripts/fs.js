var Fs = {
  this.debug = true;
  this.module = 'Fs';
  
  this.folder = function(folder){

  };

  this.file = function(name, type){

  };

  this.binary = function(name, type){

  };
};

Fs.prototype = new Ap();
